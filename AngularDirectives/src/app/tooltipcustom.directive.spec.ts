import { TooltipDirective } from './tooltipcustom.directive';

describe('TooltipcustomDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipDirective();
    expect(directive).toBeTruthy();
  });
});
